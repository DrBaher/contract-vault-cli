version: 2
updates:
  # Keep the SHA-pinned GitHub Actions current (pinning without updates goes stale).
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    commit-message:
      prefix: "ci"
    groups:
      actions:
        patterns: ["*"]
